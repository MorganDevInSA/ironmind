/**
 * Shared width for hover “peek” panels (dashboard day strip, …)
 * so the panel edge does not jump between targets.
 */
export const PEEK_PANEL_FIXED_WIDTH_CLASS = 'w-[min(16rem,calc(100vw-5rem))]';
